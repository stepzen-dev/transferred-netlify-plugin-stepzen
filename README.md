Netlify Build plugin stepzen - This plugin will deploy a stepzen API endpoint on every netlify build..

# Install

Please install this plugin from the Netlify app.

# Configuration

The following `inputs` options are available.
